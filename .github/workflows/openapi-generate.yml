name: Generate GitBook docs from OpenAPI

on:
  push:
    branches:
      - main

permissions:
  contents: write   # allows pushing to docs branch

jobs:
  generate-and-push:
    runs-on: ubuntu-latest

    steps:
      # Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Setup Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # Install deps for snippet injection (no package.json)
      - name: Install injector dependencies
        run: npm install --no-audit --no-fund --no-save js-yaml openapi-snippet

      # Make commit script executable
      - name: Make commit script executable
        run: chmod +x .github/scripts/commit_docs.sh

      # Generate, commit, and push docs
      - name: Generate API docs and push
        run: .github/scripts/commit_docs.sh
